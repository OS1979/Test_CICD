name: learn-github-actions
run-name: ${{ github.actor }} is learning GitHub Actions
on: [workflow_dispatch]
jobs:

  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: Run build script
        run: ./.github/scripts/myscripts.sh
        shell: bash
